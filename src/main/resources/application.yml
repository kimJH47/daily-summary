spring:
  application:
    name: daily-summary
  datasource:
    url: jdbc:mysql://localhost:3306/seajul_test?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false